node_modules
dist
# Object Scanner debug fixtures dropped by the dev server. A few MB of PNG
# frames each, and working material rather than part of the piece.
captures/
.DS_Store
*.local
.vite
# Scratch worktrees agents check out into. The rest of .claude (commands,
# settings) is tracked on purpose, so this has to be scoped rather than blanket.
.claude/worktrees/
